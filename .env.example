# Omit DATABASE_URL to use local embedded PostgreSQL (PGlite).
# DATABASE_URL=postgres://routing:routing@localhost:5432/routing
# Local mode uses a single Node process. Do not share this directory across processes.
PGLITE_DATA_DIR=./.data/routing
